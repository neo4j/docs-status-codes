= 42N35

== Status description
error: syntax error or access rule violation - unsupported path selector or explicit path mode in path pattern. The path selector or explicit path mode `{ <<selectorOrPathMode>> }` is not supported within quantified or parenthesized path patterns.

[#_example_scenario]
== Example scenario

Try using `SHORTEST` or an explicit path mode like `ACYCLIC` within a quantified path pattern:

[source,cypher]
----
MATCH ()(SHORTEST 5 PATHS (a)-[r]->(b))*() RETURN r;
MATCH ()(ACYCLIC (a)-[r]->(b))*() RETURN r;
----

The query returns a chain of errors where GQLSTATUS 42N35 is the cause of GQLSTATUS xref:errors/gql-errors/42001.adoc[42001]:

.GQLSTATUS error chain
[source, error]
----
42N35: syntax error or access rule violation - unsupported path selector or explicit path mode in path pattern. The path selector or explicit path mode SHORTEST 5 PATHS is not supported within quantified or parenthesized path patterns. (line 1, column 27 (offset: 26))
"MATCH ()(SHORTEST 5 PATHS (a)-[r]->(b))*() RETURN r"
                           ^
  42001: syntax error or access rule violation - invalid syntax
----

== Possible solution
Move the `SHORTEST` clause or an explicit path mode outside of the quantified path pattern:

[source,cypher]
----
MATCH SHORTEST 5 PATHS ()((a)-[r]->(b))*() RETURN r;
MATCH ACYCLIC ()((a)-[r]->(b))*() RETURN r;
----

ifndef::backend-pdf[]
[discrete.glossary]
== Glossary

include::partial$glossary.adoc[]
endif::[]