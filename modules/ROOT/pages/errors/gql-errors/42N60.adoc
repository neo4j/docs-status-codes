= 42N60

== Status description
error: syntax error or access rule violation - unsupported combination of match mode and path mode. `REPEATABLE ELEMENTS` with `{ <<pathMode>> }` path mode is not supported.

== Example scenario
For example, in the following query `REPEATABLE ELEMENTS` is used with the `TRAIL` path mode:

[source,cypher]
----
MATCH REPEATABLE ELEMENTS p = TRAIL (:A)-[:R]->{,10}(:B)
RETURN p;
----

The query returns a chain of errors where GQLSTATUS 42N60 is the cause of GQLSTATUS xref:errors/gql-errors/42001.adoc[42001]:

.GQLSTATUS error chain
[source, error]
----
42N60: syntax error or access rule violation - unsupported combination of match mode and path mode. REPEATABLE ELEMENTS with TRAIL path mode is not supported. (line 1, column 31 (offset: 30))
"MATCH REPEATABLE ELEMENTS p = TRAIL (:A)-[:R]->{,10}(:B)"
                               ^
  42001: syntax error or access rule violation - invalid syntax
----

ifndef::backend-pdf[]
[discrete.glossary]
== Glossary

include::partial$glossary.adoc[]
endif::[]